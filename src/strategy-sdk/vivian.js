import { ARENA_CONFIG } from "../config.js";import { candidateConfidence,changeFor,compatibleCandidates,eligible,manage,pass,sell } from "./runtime.js";
export const VIVIAN_METADATA=Object.freeze({strategyId:"RAFI_VIVIAN_MOMENTUM",strategyName:"Vivian Momentum Compatibility Strategy",strategyAuthor:"RA-FI / Vector handoff",strategyVersion:"CODY_MOMENTUM_COMPAT_V1.2",arenaRulesVersion:"1.1",integrationVersion:"1.0.0",creationDate:"2026-08-04",commit:"cf578f49ccee228ac80e7ecdb1697f731ee9b530"});
export function evaluateVivianStrategy({agent,candidates=[],opportunities=[],assets,round,availableSlots=ARENA_CONFIG.maximumOpenPositions,now=Date.now()}){candidates=compatibleCandidates(candidates,opportunities);const positions=Object.values(agent.positions||{}).sort((a,b)=>a.symbol.localeCompare(b.symbol));const risk=positions.find(p=>changeFor(p,assets)<=-0.8);if(risk)return sell(risk,"CODY_RISK_EXIT",86);const pressure=positions[0];if(pressure&&round.remainingSeconds<=30)return sell(pressure,"CODY_ROUND_PRESSURE",82);const target=positions.find(p=>changeFor(p,assets)>=1);if(target)return sell(target,"CODY_MOMENTUM_TARGET",84);if(availableSlots>0){const adaptation=agent.halftimeAdaptation?.roundNumber===round.number&&agent.halftimeAdaptation?.active?agent.halftimeAdaptation:null,threshold=adaptation?Math.max(ARENA_CONFIG.vivianConfidenceSafetyFloor,round.remainingSeconds<=60?adaptation.finalMinuteThreshold:adaptation.halftimeThreshold):ARENA_CONFIG.vivianOpeningConfidenceThreshold,ranked=eligible(candidates,agent,assets,now).filter(c=>Number(c.percentMove)>=0).sort((a,b)=>Number(b.percentMove)-Number(a.percentMove)||candidateConfidence(b)-candidateConfidence(a)),c=ranked.find(x=>candidateConfidence(x)>=threshold);if(c)return {decision:"TRADE",productId:c.productId,selectedOpportunityId:c.opportunityId,selectedCandidateId:c.candidateId,candidateSource:c.source,allocation:{type:"PERCENT_OF_AVAILABLE_CASH",value:25},reasonCode:adaptation?"CODY_EXPANDED_SEARCH_ENTRY":"CODY_MOMENTUM_ENTRY",reasonCodes:["FRESH_CANDIDATE",c.source,adaptation?"HALFTIME_ADAPTED":"NORMAL_THRESHOLD"],confidence:candidateConfidence(c)};}return positions.length?manage(positions[0],"CODY_MONITOR_PORTFOLIO",68):pass(availableSlots?"CODY_NO_POSITIVE_MOMENTUM":"CODY_PORTFOLIO_FULL",55);}
