import { ARENA_CONFIG } from "../config.js";import { candidateConfidence,changeFor,compatibleCandidates,eligible,manage,pass,sell } from "./runtime.js";
export const PRESTON_METADATA=Object.freeze({strategyId:"RAFI_PRESTON_REVERSION",strategyName:"Preston Adaptive Reversion Compatibility Strategy",strategyAuthor:"RA-FI / Anthropic handoff",strategyVersion:"ATLAS_REVERSION_COMPAT_V1.2",arenaRulesVersion:"1.1",integrationVersion:"1.0.0",creationDate:"2026-08-04",commit:"cf578f49ccee228ac80e7ecdb1697f731ee9b530"});
export function evaluatePrestonStrategy({agent,candidates=[],opportunities=[],assets,round,availableSlots=ARENA_CONFIG.maximumOpenPositions,now=Date.now()}){candidates=compatibleCandidates(candidates,opportunities);const positions=Object.values(agent.positions||{}).sort((a,b)=>a.symbol.localeCompare(b.symbol));const risk=positions.find(p=>changeFor(p,assets)<=-0.6);if(risk)return sell(risk,"ATLAS_RISK_EXIT",82);const pressure=positions[0];if(pressure&&round.remainingSeconds<=30)return sell(pressure,"ATLAS_ROUND_PRESSURE",78);const target=positions.find(p=>changeFor(p,assets)>=0.7);if(target)return sell(target,"ATLAS_REVERSION_TARGET",80);if(availableSlots>0){const ranked=eligible(candidates,agent,assets,now).filter(c=>Number(c.percentMove)<=0).sort((a,b)=>Number(a.percentMove)-Number(b.percentMove)||candidateConfidence(b)-candidateConfidence(a)),c=ranked[0],adaptation=agent.halftimeAdaptation?.roundNumber===round.number&&agent.halftimeAdaptation?.active?agent.halftimeAdaptation:null;if(c)return {decision:"TRADE",productId:c.productId,selectedOpportunityId:c.opportunityId,selectedCandidateId:c.candidateId,candidateSource:c.source,allocation:{type:"PERCENT_OF_AVAILABLE_CASH",value:adaptation?.allocationPercent||ARENA_CONFIG.prestonBaseAllocationPercent},reasonCode:adaptation?"ATLAS_HALFTIME_VALUE_ENTRY":"ATLAS_REVERSION_ENTRY",reasonCodes:["FRESH_CANDIDATE",c.source,adaptation?"HALFTIME_ALLOCATION":"BASE_ALLOCATION"],confidence:candidateConfidence(c)};}return positions.length?manage(positions[0],"ATLAS_MONITOR_PORTFOLIO",72):pass(availableSlots?"ATLAS_NO_OVERSOLD_ASSET":"ATLAS_PORTFOLIO_FULL",58);}
