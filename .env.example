# Runtime secrets/variables. Never commit real values.
ARENA_ADMIN_TOKEN=replace-with-a-long-random-secret
OPPORTUNITY_ENGINE_BASE_URL=https://your-opportunity-engine.workers.dev

# Reserved for a future direct CoinGecko adapter; unused by the current Worker.
COINGECKO_DEMO_KEY=replace-with-provider-key

# Non-secret execution configuration (defaults are also in wrangler.toml).
FEE_RATE_BPS=40
SLIPPAGE_BPS=5
SYNTHETIC_SPREAD_BPS=4
MAX_QUOTE_AGE_SECONDS=90
