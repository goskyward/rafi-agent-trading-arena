import assert from "node:assert/strict";
const response=await fetch("http://127.0.0.1:8790/arena");assert.equal(response.status,200);const arena=await response.json();assert.ok(arena.campaign);assert.ok(arena.agents.CODY);assert.ok(arena.agents.ATLAS);assert.doesNotMatch(JSON.stringify(arena),/boardHash|scanCycleId|agent_decisions|opportunity_scans/);console.log("Durable Object state reactivation and public-data boundary passed");
